spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: url
    username: 用户名
    password: 密码
  # session 失效时间 一天
  session:
    timeout: 86400
server:
  address: 0.0.0.0
